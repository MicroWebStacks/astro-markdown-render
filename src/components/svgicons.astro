---
import {promises as fs} from 'fs';

export interface Props {
  filename: string;
}

const { filename } = Astro.props as Props;
const innerHTML = await fs.readFile(`./src/assets/${filename}.svg`)
---

<Fragment set:html={innerHTML} />

